channel = std::get_current_channel()

if (channel == "calculate_activation_ts") {
    next = time::now() + std::get_config("interval")
    std::set_state("next_activation_ts", next)
    return
}

if (channel == "activate") {
    std::set_output("pulse", !std::get_output("pulse"))
    return
}
